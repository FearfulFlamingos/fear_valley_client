using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CharacterPanelPopulate : MonoBehaviour
{
    public Text leftStats, rightStats;
    private string leftBasic = "Name:\nAttack:\nAction Points:";
    private string rightBasic = "Class:\nDefense:\nMovement:";
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
